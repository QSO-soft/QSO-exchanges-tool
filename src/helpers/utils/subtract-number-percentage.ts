export const subtractNumberPercentage = (value: number, percent: number): number => value - (value * percent) / 100;
export const addNumberPercentage = (value: number, percent: number): number => value + (value * percent) / 100;
